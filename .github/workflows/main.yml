name: send-tweet
on:
<<<<<<< HEAD
  schedule:
    - cron: "0 */10 * * *"
=======
   schedule :
          - cron : '0 */10 * * *'
  
>>>>>>> 50c75230a22289da283309e72f78089db95cd7b6
jobs:
  tweet:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npm i
      - run: echo "${{ secrets.ENV_FILE }}" > .env
      - run: node index.js
